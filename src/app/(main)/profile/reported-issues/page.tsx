/**
 * Reported Issues page - delegates to layout which renders ReportedIssuesSection
 * based on the active tab derived from pathname.
 */
export default function ReportedIssuesPage() {
  return null;
}
