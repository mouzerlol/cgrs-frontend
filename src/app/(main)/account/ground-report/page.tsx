/**
 * Ground Report page — delegates to the account layout, which renders
 * GroundReportSection based on the active tab derived from pathname. The tab is
 * only present for owners-and-up; lower roles never reach this route via the UI.
 */
export default function AccountGroundReportPage() {
  return null;
}
