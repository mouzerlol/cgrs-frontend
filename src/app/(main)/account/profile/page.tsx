/**
 * Profile details page - delegates to the account layout which renders
 * ProfileDetailsSection based on the active tab derived from pathname.
 */
export default function ProfilePage() {
  return null;
}
