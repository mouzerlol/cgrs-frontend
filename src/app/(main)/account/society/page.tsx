/**
 * Society page - delegates to the account layout which renders SocietySection
 * based on the active tab derived from pathname. The tab is only present for
 * owners-and-up; lower roles never reach this route via the UI.
 */
export default function SocietyPage() {
  return null;
}
